package ria.intellij.psi.impl;

import com.intellij.lang.ASTNode;
import ria.intellij.psi.RiaTypes;

public class RiaPsiImplUtil {
/*    public static String getKey(RiaProperty element) {
        ASTNode keyNode = element.getNode().findChildByType(RiaTypes.KEY);
        if(keyNode != null) {
            // IMPORTANT: Convert embedded escaped spaces to simple spaces
            return keyNode.getText().replaceAll("\\\\ ", " ");
        } else {
            return null;
        }
    }

    public static String getValue(RiaProperty element) {
        ASTNode valueNode = element.getNode().findChildByType(RiaTypes.VALUE);
        if(valueNode != null) {
            return valueNode.getText();
        } else {
            return null;
        }
    }*/
}